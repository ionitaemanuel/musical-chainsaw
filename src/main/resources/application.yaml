spring:
  application:
    name: chainsaw
  graphql:
    graphiql:
      enabled: true
    websocket:
      path: /graphql
#  h2:
#    console:
#      enabled: true
#  datasource:
#    url: jdbc:h2:mem:testdb;NON_KEYWORDS=user
#    username: sa
#    password: password
#    driverClassName: org.h2.Driver
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#    defer-datasource-initialization: true
#    hibernate:
#      ddl-auto: update
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test
      uri: jdbc:mongodb://localhost:27017/test
  liquibase:
    change-log: classpath:db-xml/master.xml
    url: jdbc:mongodb://localhost:27017/test?retryWrites=true