Task 3:

Replace get user by id with GraphQl
Replace the user update endpoint defined already, with one generated by Open api ( use mapstruct to map the domain user to a DTO)
Add a websockets conention to get stats from aop counting.


You can change the queries/endpoints you want to implement, just make sure to include all new things.